// Declare a variable
let count: number = 0;

// Declare a constant
const MAX_COUNT: number = 10;

// Update the variable
count = 5;

// Print the values
console.log("Count:", count);
console.log("Max Count:", MAX_COUNT);
